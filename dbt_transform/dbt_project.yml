name: 'dbt_transform'
version: '1.0.0'
profile: 'dbt_transform'

model-paths: ['models']
seed-paths: ['seeds']
test-paths: ['tests']
analysis-paths: ['analyses']
macro-paths: ['macros']

target-path: 'target'
clean-targets:
  - 'target'
  - 'dbt_packages'

models:
  dbt_transform:
    staging:
      +materialized: view
    marts:
      +materialized: table
